1.Generate package.json
    npm init -y

2.Create server.js

3.install , import ecpress and create http server. Assign Port

###connect MongoDB database
        REST-API ---mongodb native driver----
        PEST API ---MongoDB OOm tool(MoNGOOSE)----mongodb server

        a.install and connect to mongodb server
        b.create schema of resource
        c.create model of  that schema
        d.perform DB operation on that schema


        POST/products
        GET/products
        GET/products<id>
        PUT/products<id>